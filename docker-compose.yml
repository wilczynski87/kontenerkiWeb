services:
  kotlin-wasm-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - GRADLE_OPTS=-Dorg.gradle.daemon=false
    container_name: kontenerki-web
    ports:
      - "${PORT:-80}:80"  # Allows overriding port via environment variable
    restart: unless-stopped
    networks:
      - web-network
#    healthcheck:
#      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80"]
#      interval: 30s
#      timeout: 10s
#      retries: 3
#      start_period: 30s
#    logging:
#      driver: "json-file"
#      options:
#        max-size: "10m"
#        max-file: "3"

#  api:
#    build:
#      context: ../KONTENERKIAPI
#      dockerfile: Dockerfile
#    container_name: kontenerkiapi
#    restart: unless-stopped
#    networks:
#      - web-network

networks:
  web-network:
    driver: bridge